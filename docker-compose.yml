services:
  openclaw-board:
    image: ghcr.io/dennisbakhuis/openclaw-board:latest
    ports:
      - "4321:4321"
    volumes:
      - ./tickets:/data/tickets
    environment:
      - TICKETS_DIR=/data/tickets
    restart: unless-stopped

  openclaw-board-local:
    build: .
    ports:
      - "4321:4321"
    volumes:
      - ./tickets:/data/tickets
    environment:
      - TICKETS_DIR=/data/tickets
    restart: unless-stopped
    profiles:
      - local
